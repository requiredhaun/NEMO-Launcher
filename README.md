# Nema Launcher

Nothing-style Minecraft лаунчер: dot-matrix сетка, глиф-анимации, монохром + red accent.

## dev

```bash
npm install
npm run dev
```

## build

```bash
npm run dist
```

Данные: `%APPDATA%/nema-launcher/` — config, `instances/<id>/`, java рантаймы.
